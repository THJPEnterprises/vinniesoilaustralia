/* Vinnie's Oil Australia — Portal / Supabase config
   Fill in these two values from your Supabase project:
   Project Settings → API → Project URL / anon public key.
   The anon key is safe to expose in client code — it only allows
   what your Row Level Security policies permit. */

window.SUPABASE_URL = 'https://syajrsonkwhpfyikkugw.supabase.co';
window.SUPABASE_ANON_KEY = 'sb_publishable_INsg1zpeFM3-kLvIc2LcCw_RbbKs--z';
